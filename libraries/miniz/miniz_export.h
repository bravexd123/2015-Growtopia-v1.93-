#pragma once
/* Stub for static-build use (we compile miniz.c directly into the target
   instead of using miniz's own CMake build, which normally generates this
   file via GenerateExportHeader). No dllexport/dllimport needed here. */
#define MINIZ_EXPORT
